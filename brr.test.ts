import { assertEquals } from "https://deno.land/std@0.105.0/testing/asserts.ts";

Deno.test({
  name: "I will write a real test later!",
  fn: () => {
    const a = 10 + 32;

    assertEquals(a, 42);
  },
});
